{
  "name": "admin-dashboard",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "npm run build --prefix frontend && npm run start --prefix backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
